#pragma once
#include <math.h>
//stale
const long long minStandardStarRadius = 150000000;
const long long maxStandardStarRadius = 7000000000;
const long long minPlanetRadius = 2000000;
const long long maxPlanetRadius = 100000000;
const long long minMoonRadius = 500000;
const long long maxMoonRadius = 2000000;
const long long minMoonDistance = 200000000;
const long long maxMoonDistance = 2000000000;
const long long minDistance = 100000000000;
const long long maxDistance = 600000000000;
const long long maxPlanets = 5;
const long long maxMoons = 5;
const long double MinStarMassPerMeterRadius = 20*pow(10,20);
const long double MaxStarMassPerMeterRadius = 60 * pow(10, 20);
const long double MinPlanetMassPerMeterRadius = pow(10, 18);
const long double MaxPlanetMassPerMeterRadius = 3 * pow(10, 19);
const long double G = 6.67*pow(10, -11);
const long double PI = 3.14159265;
